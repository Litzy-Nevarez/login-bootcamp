import React, { useState } from 'react';
import './../styles/dashboard.css';

const Dashboard = () => {

    return (
        <div className="container">
            {/* Contenido principal */}
            <main className='main-container'>
                <h1 className='titulo'>Bienvenido(a) al Dashboard</h1>
            </main>
        </div>
    );
};

export default Dashboard;
